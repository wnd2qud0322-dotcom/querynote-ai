# QueryNote AI — 팀 계획서
**제3회 숭실 재학생 학습법 경진대회**

---

## 📌 주제 선정 배경

데이터베이스 같은 전공 수업에서 영어로 된 강의자료 PDF를 보며 공부할 때 이런 불편함이 있었다.

- LMS에서 받은 PDF를 보다가 모르는 개념이 생기면 → AI에게 페이지 내용을 **일일이 복붙**해서 질문
- AI 설명을 받으면 → 그걸 **아이패드에 따로 필기**
- 나중에 복습할 땐 **강의자료 + 필기 두 개를 왔다갔다** 해야 함
- SQL 코드가 슬라이드 이미지로 삽입되어 있어 직접 **실행해볼 수 없음**
- 시험 전에 어디서 틀렸는지, 어떤 개념이 약한지 **추적이 안 됨**

→ **"강의자료를 보면서, 그 자리에서 바로 AI와 함께 학습할 수 있는 도구"** 가 있으면 이 모든 불편함이 해결된다.

---

## 💡 아이디어 개요

**QueryNote AI** — DB 전공 강의자료 기반 AI 인터랙티브 학습 노트

| 항목 | 내용 |
|---|---|
| 학습 영역 | 교과 학습 (전공) |
| 활용 유형 | 개념탐구형 + 메타인지형 |
| 대상 | 데이터베이스 전공 수업 수강생 (전 학과 범용 적용 가능) |

---

## 🛠 핵심 기능

### 1. PDF 강의자료 뷰어 + 즉흥 필기
- 강의자료 PDF를 업로드하면 원본 화면 그대로 표시
- 수업 중 교수님 설명 들으면서 **즉흥적으로 필기** 입력 가능
- 필기 입력 후 **AI가 자동으로 깔끔하게 정리** (불릿 형태)
- 필기는 **페이지 번호와 함께 저장** → 나중에 열면 그 페이지 필기가 그대로 복원

### 2. SQL 실습 탭 — 코드 수정 → 테이블 즉시 시각화
- PDF 속 SQL 코드를 자동 추출해 실습 탭으로 바로 전송
  - PDF 텍스트가 줄/열 단위로 깨져 있어도 `SELECT`, `FROM`, `JOIN`, `ON` 등의 SQL 블록만 분리
  - SQL이 이미지로 들어간 슬라이드는 페이지를 고해상도 캔버스로 렌더링한 뒤 OCR로 코드 인식
  - 강의자료처럼 SQL 옆에 설명 문구가 함께 배치된 경우에도 코드 부분만 정리
  - OCR 결과는 SQL parser로 문법 검증 후 자동 보정하고, 실패 시 수정 필요 지점을 표시
- 지원 SQL 범위:
  - **DDL**: `CREATE TABLE`, `ALTER TABLE`, `DROP TABLE`
  - **DML**: `INSERT INTO`, `UPDATE SET WHERE`, `DELETE FROM WHERE`, `SELECT (WHERE / BETWEEN / IN / DISTINCT / LIKE / ORDER BY / AS)`
  - **JOIN 실습**: `NATURAL JOIN`, `INNER JOIN`, `CROSS JOIN`, `LEFT OUTER JOIN`, `RIGHT OUTER JOIN`, `SELF JOIN`
  - 제약조건: `PRIMARY KEY`, `FOREIGN KEY`, `NOT NULL`, `UNIQUE`
- 코드 실행 시 **테이블 구조(컬럼/자료형/제약조건)**, **실제 데이터 행 변화**, **JOIN 결과 테이블**을 시각화
  - INSERT → 새 행 초록색 강조 / UPDATE → 변경된 행 노란색 강조 / DELETE → 해당 행 제거
  - JOIN → Student, Department, Employee 등 학습용 샘플 테이블을 기준으로 결과 행 표시
- 실행 후 **AI가 핵심 포인트 해설 자동 생성**
- 실행 로그 탭 — 순서대로 실행된 SQL 내역 확인

### 3. ERD 탭 — SQL 스키마 기반 ERD 자동 생성
- `CREATE TABLE` 코드 실행 시 **ERD 미리보기 자동 생성**
  - 테이블명, 컬럼, PK/FK 관계 시각화
  - `FOREIGN KEY` 설정 시 테이블 간 관계선 표시
- ERD를 보면서 Entity Set → 테이블 변환, 1:1 / 1:N / M:N 관계를 코드로 직접 만들어보며 이해
- 강의에서 배운 ER 다이어그램 개념을 실제 SQL로 구현해보는 연습 가능

### 4. 회독 단계별 학습 모드
- **1회독**: AI 설명 자세하게, 퀴즈 잠금 → 개념 이해 집중
- **2회독**: 실습 + 퀴즈 개방 → 적용 단계
- **3회독+**: AI 설명 압축, 심화 퀴즈 추가 → 빠른 점검

### 5. 퀴즈 + 오개념 자동 추적
- 회독 단계별 객관식 퀴즈 제공
- 오답 선택 시 **AI 해설 즉시 생성** + 관련 PDF 페이지 연결
- 오답 개념은 **약점 노트에 자동 저장** → 반복 복습 유도

### 6. 서재 — 강의자료별 학습 이력 관리
- 업로드한 강의자료가 서재에 카드로 저장
- 마지막 학습 페이지, 필기 수, 약점 개념 수, 회독 진행률 한눈에 확인
- "이어서 학습하기"로 이전 상태 그대로 복원

### 7. 약점 노트
- 퀴즈 오답 / 재설명 요청 / 필기 저장 내용 통합 관리
- 개념별로 오답 횟수, 날짜, 관련 PDF 페이지 표시
- "PDF로 이동 / SQL 실습 보기 / 다음 회독 추천" 연결

---

## 🎯 차별화 포인트

1. **단순 챗봇이 아님** — PDF를 보면서 그 자리에서 바로 AI + 실습 + 필기가 연결되는 **통합 학습 흐름**
2. **범용 도구** — 데이터베이스 과목 기준으로 만들었지만 어떤 전공 PDF든 업로드해서 사용 가능
3. **오개념 추적** — 한 번 보고 끝이 아니라 회독할수록 내 약점이 쌓이고 정리됨
4. **실제 페인포인트에서 출발** — 기획자가 직접 겪은 불편함을 해결하는 도구

---

## 🖥 구현 방식

- Claude API 활용 (필기 자동 정리, SQL 해설, 퀴즈 해설)
- React 기반 웹앱 (Claude Artifacts)
- PDF.js로 강의자료 원본 렌더링
- window.storage로 학습 데이터 영구 저장

---

## 📅 진행 계획

| 기간 | 내용 |
|---|---|
| ~7/3 | 예선 신청서 제출 + 프로토타입 완성 |
| 7/7 | 본선 진출 여부 확인 |
| 7/11~23 | 멘토링 + 재학생 5인 이상 테스트 |
| 8/5 | 본선 발표 |

---

## 👥 역할 분담 (예시)

| 역할 | 담당 |
|---|---|
| 전체 기획 / 개발 | 소정 |
| 발표 / 디자인 보완 | 팀원 |

---

*예선 제출 마감: 2026년 7월 3일(금) 17:00*
